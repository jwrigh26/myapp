import Typography from "@mui/material/Typography";
import PageLayout from "layouts/PageLayout";

export default function ModularArithmetic() {
  return (
    <PageLayout>
      <Typography variant="h1">Application in Hashing</Typography>
      <Typography variant="h2">Modular Inverses</Typography>
    </PageLayout>
  );
}
